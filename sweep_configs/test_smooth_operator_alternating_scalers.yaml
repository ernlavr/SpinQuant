method: grid # grid, bayes, random
metric:
  name: 'compressed/svd_ppl'
  goal: 'minimize'

parameters:
  param_ratio_target:
    values:
      - 0.9
      - 0.75
      - 0.5
  train_svd_scalers_sequentially:
    values:
      - True
      - False