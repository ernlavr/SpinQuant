method: grid # grid, bayes, random
metric:
  name: 'student_ppl'
  goal: 'minimize'

parameters:
  param_ratio_target:
    values:
      - 0.9
      - 0.75
      - 0.5
      - 0.25
  
  use_distillation:
    values:
      - True
      - False

  # use_alternating_LR_training:
  #   values:
  #     - True
  #     - False

  # train_per_layer:
  #   values:
  #     - True
  #     - False
