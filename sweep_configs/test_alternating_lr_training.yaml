method: grid # grid, bayes, random
metric:
  name: 'student_ppl'
  goal: 'minimize'

parameters:
  param_ratio_target:
    values:
      - 0.9
      - 0.75
      - 0.66
      - 0.5
      - 0.25
  
  use_alternating_LR_training:
    values:
      - True
      - False