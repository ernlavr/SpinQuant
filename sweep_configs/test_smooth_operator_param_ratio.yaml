method: grid # grid, bayes, random
metric:
  name: 'compressed/svd_ppl'
  goal: 'minimize'

parameters:
  param_ratio_target:
    values:
      - 0.75
      - 0.5
      - 0.25
  train_low_rank_smoothing:
    values:
      - True
      - False