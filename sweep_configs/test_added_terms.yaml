method: grid # grid, bayes, random
metric:
  name: 'loss'
  goal: 'minimize'

parameters:
  add_term_to_loss:
    values:
      - entropy
      - participation

  term_loss_scaler:
    values:
      - 0.000001
      - 0.00001
      - 0.0001
      - 0.001
      - 0
